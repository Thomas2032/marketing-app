# Mock mode (default) — full UI with sessionStorage, no backend required
# NEXT_PUBLIC_USE_BACKEND=false

# Live API — backend must be running on port 8000
NEXT_PUBLIC_USE_BACKEND=False
NEXT_PUBLIC_API_URL=http://localhost:8000/api/v1
