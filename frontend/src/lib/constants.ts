export const DEMO_USER_ID = "demo-user";

/** `true` = live API. `false` = mock data with the full connected UI (no backend required). */
export const USE_BACKEND = process.env.NEXT_PUBLIC_USE_BACKEND !== "false";
